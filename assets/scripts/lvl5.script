local game = require("assets.scripts.game")

function init(self)
	self.sound = false
	if game.death == 0 then
		sound.play("#s1", {}, nil)
	end
end

function on_message(self, message_id, message, sender)
	if message_id == hash("door_open") and not self.sound then
		self.sound = true
		sound.stop("#s1", {})
		sound.play("#s2", {}, function ()
			sound.play("#s3", {}, nil)
		end)
	end
end