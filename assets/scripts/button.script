function on_message(self, message_id, message, sender)
	if message_id == hash("contact_point_response") and message.own_group == hash("trigger") then
		msg.post("#sprite", "play_animation", {id = hash("button")})
	end
end